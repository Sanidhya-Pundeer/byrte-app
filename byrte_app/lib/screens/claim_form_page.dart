import 'package:flutter/material.dart';

class ClaimFormPage extends StatelessWidget {
  const ClaimFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Claim Form Page",
        style: TextStyle(fontSize: 18, fontWeight: FontWeight.w500),
      ),
    );
  }
}
